### Error messages for the IndexChecker
array.access.unsafe.high=Potentially unsafe array access: only use @IndexFor("%s") as index. Found: "%s" which could be too high
array.access.unsafe.name=Potentially unsafe array access: only use @IndexFor("%s") as index. Found: "%s" which has the wrong name
array.access.unsafe=Potentially unsafe array access: only use @IndexFor("%s") as index. Found: "%s"
array.access.unsafe.low=Potentially unsafe array access: only use @IndexFor("%s") as index. Found: "%s" which could be too low
list.access.unsafe.high=Potentially unsafe list access: only use @IndexFor("%s") as index. Found: "%s" which could be too high
list.access.unsafe.low=Potentially unsafe list access: only use @IndexFor("%s") as index. Found: "%s" which could be too low
list.access.unsafe=Potentially unsafe list access: only use @IndexFor("%s") as index. Found: "%s"
list.access.unsafe.name=Potentially unsafe list access: only use @IndexFor("%s") as index. Found: "%s" which has the wrong name
string.access.unsafe.high=Potentially unsafe string access: only use @IndexFor("%s") as index. Found: "%s" which could be too high
string.access.unsafe.low=Potentially unsafe string access: only use @IndexFor("%s") as index. Found: "%s" which could be too low
string.access.unsafe=Potentially unsafe string access: only use @IndexFor("%s") as index. Found: "%s"
string.access.unsafe.name=Potentially unsafe string access: only use @IndexFor("%s") as index. Found: "%s" which has the wrong name
