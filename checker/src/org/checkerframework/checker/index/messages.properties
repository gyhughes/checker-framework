### Error messages for the IndexChecker
array.access.unsafe.high=Potentially unsafe array access: the index could be too high\nfound   : %s\nrequired: @IndexFor("%s") int
array.access.unsafe.name=Potentially unsafe array access: the value is an index for the wrong array\nfound   : %s\nrequired: @IndexFor("%s") int
array.access.unsafe=Potentially unsafe array access\nfound   : %s\nrequired: @IndexFor("%s") int
array.access.unsafe.low=Potentially unsafe array access: the index could be negative\nfound   : %s\nrequired: @IndexFor("%s") int
list.access.unsafe.high=Potentially unsafe list access: the index could be too high\nfound   : %s\nrequired: @IndexFor("%s") int
list.access.unsafe.low=Potentially unsafe list access: the index could be negative\nfound: %s\nrequired: @IndexFor("%s") int
list.access.unsafe=Potentially unsafe list access\nfound   : %s\nrequired: @IndexFor("%s") int
list.access.unsafe.name=Potentially unsafe list access: the value is an index for the wrong list\nfound   : %s\nrequired: @IndexFor("%s") int
string.access.unsafe.high=Potentially unsafe string access: the index could be too high\nfound   : %s\nrequired: @IndexFor("%s") int
string.access.unsafe.low=Potentially unsafe string access: the index could be negative\nfound   : %s\nrequired: @IndexFor("%s") int
string.access.unsafe=Potentially unsafe string access\nfound   : %s\nrequired: @IndexFor("%s") int
string.access.unsafe.name=Potentially unsafe string access: the value is an index for the wrong string\nfound   : %s\nrequired: @IndexFor("%s") int
